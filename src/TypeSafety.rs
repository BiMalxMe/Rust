fn main(){
    let x = 1;
    x = "Bimal";
    println!(x);
}

//in this program we basically see the type error while compiling
// like the typescrpt and all, because the x variable is declared 
// a integer value at first and then after changing the data type
//invoked the error due to which ERROR occurs